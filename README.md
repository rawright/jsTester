jsTester
========

Learning TDD by building a testing tool for JavaScript

Use Jasmine syntax:
* describe
* it
* expect
* expect().not() - workaround for now
* toBe
* toBeArray
* toBeFunction
* toBeNull
* toBeNumber
* toBeObject
* toBeString
* toBeUndefined
